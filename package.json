{
  "name": "streaming-alert-workspace",
  "version": "1.0.0",
  "description": "Root project for StreamNeon-like streaming alert platform",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix frontend",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
